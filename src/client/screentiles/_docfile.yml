---
name: screentiles
invocations:
- rettype: function
  args:
  - type: function
    name: order
    optional: true
  - type: number
    name: range
    optional: true
  - type: function
    name: modifier
    optional: true
  description: Returns an iteration loop between screen tiles coordinates. Optionally
    you can set a <i>order</i> sorting function to sort tiles (ORDER_RADIAL, ORDER_RADIAL_INVERSE,
    ORDER_RANDOM, ORDER_EUCLIDEAN, ORDER_EUCLIDEAN_INVERSE, ORDER_REALDIST, ORDER_REALDIST_INVERSE
    or made sort function). Optionally you can set the <i>range</i> distance get coordinates.
    Optionally you can add a <i>modifier</i> function to modify use the coordinates
    as parameter and return the value generated by the function.
- rettype: function
  args:
  - type: function
    name: order
    optional: true
  - type: table
    name: positions
    optional: true
  - type: function
    name: modifier
    optional: true
  description: Returns an iteration loop between screen tiles coordinates. Optionally
    you can set a <i>order</i> sorting function to sort tiles (ORDER_RADIAL, ORDER_RADIAL_INVERSE,
    ORDER_RANDOM, ORDER_EUCLIDEAN, ORDER_EUCLIDEAN_INVERSE, ORDER_REALDIST, ORDER_REALDIST_INVERSE
    or made sort function). Optionally you can set the <i>range</i> distance get coordinates.
    Optionally you can add a <i>modifier</i> function to modify use the coordinates
    as parameter and return the value generated by the function.
