---
name: withdrawitems
deprecated: true
invocations:
- rettype: boolean
  args:
  - type: string
    name: maincont
  - type: string
    name: seccont
    optional: true
  - type: string
    name: item¹, item², item*, ...
  - type: function
    name: delayFunc
    optional: true
  description: Withdraw items from the <i>maincont</i> to the <i>seccont</i>. Returns
    true if successful, false otherwise.
- rettype: boolean
  args:
  - type: string
    name: maincont
  - type: table
    name: item¹, item², item*, ...
  - type: function
    name: delayFunc
    optional: true
  description: Withdraw items from the <i>maincont</i> to the backpack given on each
    item parameter. Returns true if successful, false otherwise.
